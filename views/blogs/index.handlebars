{{#each blogs}}
  <div class="card mb-2">
    <div class="card-block">
      <h4 class="card-title">{{title}}</h4>
      <p class="card-text text-truncate">{{stripTags content}}</p>
      <a href="#" class="btn btn-outline-primary">Read More</a>
    </div>
  </div>
{{else}}
  <p>No blogs listed. How about post your first blog?</p>
{{/each}}
